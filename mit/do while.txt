#include <stdio.h>

int main() {
    int n,i = 2;
    printf("ใส่เลขตรงนี้ : ");
    scanf("%d", &n);
    
    i = n;
    do {
        int j = 2, P = 1;
        
            while (j < i) {
                if (i % j == 0){
                P = 0;
                break;
                }
            j++;
        }
        if(P && i >= 2){
        printf("%d  ", i);
    }
    i--;
    }while (i >= 2);
    return 0;
}